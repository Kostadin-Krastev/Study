/*
Write a function that takes integer 'a’ and calculates the square area with side 'a'.
 */

const calculateSquare = function (a) {
  const calculation = a * a;

  console.log(calculation);
};

calculateSquare(10);
