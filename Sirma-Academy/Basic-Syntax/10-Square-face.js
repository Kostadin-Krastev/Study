/*
Write a function that takes integer 'a’ and calculates the square area with side 'a'. Guidelines:
Initialize an area variable in which to record the value for the rectangle face obtained
by the side*side formula. Print the result.
 */

function calcSquareArea(numA) {
  const area = numA * numA;
  console.log(area);
}

calcSquareArea(10);
